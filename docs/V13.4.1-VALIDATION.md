# DadoFit V13.4.1 — Final Polish Hotfix

Hotfix visual para cerrar la validación integrada de RC2.

## Cambios

- Squads: elimina la zona vacía del hero de métricas.
- Organizaciones: termina la localización a español.
- Retos del Gym: reemplaza labels heredados en inglés.
- Sponsored Challenges: pluralización correcta (`1 minuto`, `2 minutos`, `1 repetición`, etc.).
- Mantiene intactos Supabase, RLS, RPC, rewards, auditoría, evidencias y anti-farming.

## Validación rápida

```bash
npm test
npm run build
npm run dev
```

Revisar:

1. `/squads`: solo dos métricas en el hero, sin bloque vacío.
2. `/organizations`: título `Organizaciones` y eyebrow en español.
3. `/organization-challenges`: eyebrow `RETOS DEL GYM` y enlace `Organizaciones`.
4. `/sponsored-challenges`: `Plancha · 1 minuto` / `20 km` sin pluralización incorrecta.

No requiere migración Supabase.
