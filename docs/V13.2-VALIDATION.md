# V13.2 validation

1. Apply migration `20260827213000_sponsored_goal_types.sql`.
2. `npm test` should include the new sponsored goal tests.
3. In Brand workspace create: custom activity `Trotar`, goal `Distancia`, value `20`, unit `km`.
4. Publish the challenge and open it from a Personal workspace.
5. It must display `20 km` and `Trotar`, not `1× Trotar`.
6. Also verify one repetitions challenge remains compatible.
