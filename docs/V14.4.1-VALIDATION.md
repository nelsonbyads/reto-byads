# DadoFit V14.4.1 — Analytics schema reconciliation

## Why this exists
`20260902163000` is already present in both Local and Remote migration history, but the remote `get_dadofit_schema_version()` still reports V14. This means the migration version was applied before the local V14.4 SQL reached its current content. Supabase does not re-run an already-recorded migration version after the file changes.

## Apply
```bash
npx supabase migration list
npx supabase db push
node scripts/check-supabase.mjs
```

Expected final marker:
```text
Schema: v14.4-ad-analytics-click-tracking
```

The new migration is idempotent and reasserts:
- `ad_events`
- `record_ad_event(...)`
- `admin_ad_analytics_summary(...)`
- `admin_ad_campaign_metrics(...)`
- `admin_recent_ad_clicks(...)`
- `get_dadofit_schema_version()`

Do not delete or edit the already-applied `20260902163000` migration.
